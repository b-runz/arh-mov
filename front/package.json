{
  "name": "arh-mov",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "html-minifier-terser --file-ext html --input-dir src --output-dir dist --collapse-whitespace --remove-comments --minify-js true --minify-css true",
    "webpack": "webpack"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
